CREATE TABLE PRICES (id BIGINT NOT NULL AUTO_INCREMENT, brand_id SMALLINT NOT NULL, start_date DATETIME NOT NULL, end_date DATETIME NOT NULL, price_list SMALLINT NOT NULL, product_id INTEGER NOT NULL, priority SMALLINT NOT NULL, price DECIMAL(6,2) NOT NULL, currency_code CHAR(3) NOT NULL, PRIMARY KEY (id))
